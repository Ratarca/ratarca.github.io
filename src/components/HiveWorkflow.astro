<div class="hive-flow">
  <input class="motion-check" type="checkbox" id="hive-motion" />
  <label class="motion-control" for="hive-motion"><span aria-hidden="true"></span>Pause animation</label>
  <div class="flow-panels">
    <figure class="flow-panel" aria-labelledby="coordination-heading">
      <figcaption class="panel-heading">
        <div class="heading-title"><span class="loop-number">01</span><div><p class="overline">The coordination loop</p><h3 id="coordination-heading">One goal. Shared direction.</h3></div></div>
        <p>Define the work, give it an owner,<br class="desktop-break" /> and send the right context.</p>
      </figcaption>
      <ol class="coordination-lane" aria-label="Coordination stages in dependency order">
        <li class="flow-step gateway">
          <div class="node-card">
            <span class="node-mark gateway-mark" aria-hidden="true"><i></i><i></i><i></i></span>
            <span class="node-meta">Coordinator</span><h4>Design the gateway</h4><p>Goal, scope, dependencies, and a definition of done.</p>
          </div>
          <span class="connector" aria-hidden="true"><i></i></span>
        </li>
        <li class="flow-step context">
          <div class="node-card">
            <span class="node-mark packet-mark" aria-hidden="true"><i></i></span>
            <span class="node-meta">Task packet</span><h4>Package the context</h4><p>Relevant contracts, files, and previous evidence.</p>
          </div>
          <span class="connector" aria-hidden="true"><i></i></span>
        </li>
        <li class="flow-step specialists">
          <div class="worker-pair">
            <div class="worker"><span class="worker-dot" aria-hidden="true"></span><div><h4>Specialist A</h4><p>One bounded task</p></div></div>
            <div class="worker"><span class="worker-dot secondary" aria-hidden="true"></span><div><h4>Specialist B</h4><p>Independent write scope</p></div></div>
          </div>
          <span class="worker-note">Up to two parallel writers</span>
          <span class="connector" aria-hidden="true"><i></i></span>
        </li>
        <li class="flow-step candidate">
          <div class="node-card">
            <span class="node-mark evidence-mark" aria-hidden="true">✓</span>
            <span class="node-meta">Ready for review</span><h4>Bring the evidence</h4><p>A candidate, test results, and a clear handoff to QA.</p>
          </div>
        </li>
      </ol>
      <div class="return-path"><span aria-hidden="true">↶</span><p><strong>Back to the gateway</strong> — the coordinator records progress and selects the next ready task.</p></div>
    </figure>
    <div class="between-loops" aria-hidden="true"><span></span><p>Candidate + evidence</p><span>↓</span></div>
    <figure class="flow-panel quality" aria-labelledby="quality-heading">
      <figcaption class="panel-heading">
        <div class="heading-title"><span class="loop-number">02</span><div><p class="overline">The evidence loop</p><h3 id="quality-heading">Build. Check. Earn the next step.</h3></div></div>
        <p>Independent review connects<br class="desktop-break" /> the work to a verified result.</p>
      </figcaption>
      <div class="quality-layout">
        <div class="iteration">
          <p class="lane-label">Inside the specialist’s task</p>
          <ol class="iteration-lane">
            <li><span class="step-index">01</span><h4>Implement</h4><p>A focused change</p></li>
            <li><span class="step-index">02</span><h4>Test</h4><p>Check the boundary</p></li>
            <li><span class="step-index">03</span><h4>Record</h4><p>Keep the evidence</p></li>
          </ol>
          <div class="small-return"><span aria-hidden="true">↶</span> Test fails? Repair and test again.</div>
        </div>
        <span class="review-connector" aria-hidden="true"><i></i></span>
        <div class="qa-card"><span class="qa-seal" aria-hidden="true">✓</span><span class="node-meta">A separate reviewer</span><h4>Independent QA</h4><p>Run acceptance. Inspect the consumer. Verify the evidence.</p></div>
      </div>
      <div class="decision-paths">
        <div class="decision repair"><span class="decision-symbol" aria-hidden="true">↶</span><div><span class="decision-label">FAIL / return to the task</span><h4>Repair → retest → review</h4><p>A linked fix in the same worktree, with new evidence.</p></div></div>
        <div class="decision deliver"><span class="decision-symbol" aria-hidden="true">↗</span><div><span class="decision-label">PASS / move forward</span><h4>Integrate → final acceptance</h4><p>Verify the target, archive the receipt, open the next gateway.</p></div></div>
      </div>
    </figure>
    <div class="memory-note"><span class="memory-icon" aria-hidden="true"><i></i><i></i><i></i></span><p><strong>The thread that holds it together.</strong> Contracts, checkpoints, and evidence stay with the work through every handoff.</p></div>
  </div>
</div>

<style>
  .hive-flow { --flow-ink: #14213d; --flow-muted: #675b4b; --flow-line: #dfd6c8; --flow-gold: #d58917; position: relative; margin-top: 40px; color: var(--flow-ink); }
  .motion-check { position: absolute; width: 1px; height: 1px; clip-path: inset(50%); overflow: hidden; }
  .motion-control { display: flex; align-items: center; gap: 9px; width: fit-content; min-height: 44px; margin: 0 0 12px auto; font-size: 12px; color: var(--flow-muted); cursor: pointer; }
  .motion-control > span { display: grid; place-items: center; width: 17px; height: 17px; border: 1px solid #9b8b75; border-radius: 4px; }
  .motion-check:checked + .motion-control > span::after { content: '✓'; color: var(--flow-ink); font-size: 12px; }
  .motion-check:focus-visible + .motion-control { outline: 2px solid var(--flow-gold); outline-offset: 5px; border-radius: 3px; }
  .flow-panel { margin: 0; border: 1px solid var(--flow-line); border-radius: 22px; padding: 30px; background: #fff; box-shadow: 0 8px 26px #3d2a1105; }
  .panel-heading { display: flex; justify-content: space-between; align-items: center; gap: 24px; }
  .heading-title { display: flex; align-items: center; gap: 16px; }
  .loop-number { display: grid; place-items: center; flex-shrink: 0; width: 43px; height: 43px; border: 1px solid #e8ce9c; border-radius: 50%; background: #fff8e9; color: #93600e; font: 12px var(--mono); }
  .overline { margin: 0 0 5px; font: 10px var(--mono); letter-spacing: .07em; text-transform: uppercase; color: var(--flow-muted); }
  .panel-heading h3 { margin: 0; font: 600 clamp(21px, 2.5vw, 28px)/1.15 var(--serif); letter-spacing: -.04em; }
  .panel-heading > p { margin: 0; color: var(--flow-muted); font-size: 12px; line-height: 1.65; }
  .coordination-lane { display: grid; grid-template-columns: repeat(4, minmax(0, 1fr)); gap: 30px; padding: 0; margin: 38px 0 26px; list-style: none; align-items: center; }
  .flow-step { position: relative; min-width: 0; }
  .node-card { min-height: 211px; padding: 22px 15px 20px; border: 1px solid var(--flow-line); border-radius: 13px; background: #fffdfa; }
  .gateway .node-card { background: #fff5de; border-color: #e6c991; }
  .node-meta { display: block; margin: 17px 0 7px; color: var(--flow-muted); font: 9px var(--mono); letter-spacing: .035em; text-transform: uppercase; }
  h4 { font-size: 14px; font-weight: 600; line-height: 1.4; margin: 0 0 8px; letter-spacing: -.02em; }
  .node-card p, .worker p { margin: 0; color: var(--flow-muted); font-size: 12px; line-height: 1.65; }
  .node-mark { display: flex; align-items: flex-end; justify-content: center; gap: 3px; width: 30px; height: 30px; color: #a2670b; }
  .gateway-mark { padding: 5px 2px; }
  .gateway-mark i { width: 5px; height: 10px; border: 1px solid currentColor; border-radius: 1px; }
  .gateway-mark i:nth-child(2) { height: 20px; background: #efbb58; }
  .gateway-mark i:nth-child(3) { height: 15px; }
  .packet-mark { position: relative; border: 1px solid #8d9bb0; border-radius: 3px; width: 22px; height: 26px; margin: 2px 0; box-shadow: 4px -3px 0 #e8edf4; }
  .packet-mark i { position: absolute; top: 8px; left: 4px; right: 4px; height: 1px; background: #8d9bb0; box-shadow: 0 5px 0 #8d9bb0, 0 10px 0 #8d9bb0; }
  .evidence-mark { align-items: center; border-radius: 50%; border: 1px solid #9cb8a5; color: #477454; font-size: 18px; }
  .connector { position: absolute; left: 100%; top: 50%; width: 30px; height: 1px; background: #cdbc9e; }
  .connector::after, .review-connector::after { content: ''; position: absolute; right: 1px; top: -3px; width: 6px; height: 6px; border-right: 1px solid #b19972; border-top: 1px solid #b19972; transform: rotate(45deg); }
  .connector i, .review-connector i { position: absolute; width: 5px; height: 5px; border-radius: 50%; background: var(--flow-gold); top: -2px; left: 0; animation: handoff 5s ease-in-out infinite; }
  .context .connector i { animation-delay: .6s; }
  .specialists .connector i { animation-delay: 1.2s; }
  .worker-pair { display: grid; gap: 12px; position: relative; }
  .worker-pair::before { content: ''; position: absolute; top: 25%; bottom: 25%; left: -8px; width: 7px; border: 1px solid #cdbc9e; border-right: 0; border-radius: 4px 0 0 4px; }
  .worker-pair::after { content: ''; position: absolute; top: 25%; bottom: 25%; right: -8px; width: 7px; border: 1px solid #cdbc9e; border-left: 0; border-radius: 0 4px 4px 0; }
  .worker { display: flex; align-items: center; gap: 10px; padding: 17px 12px; border: 1px solid #d6dfeb; border-radius: 11px; background: #f5f8fc; }
  .worker:nth-child(2) { border-color: #ded8e8; background: #f8f5fb; }
  .worker h4 { margin-bottom: 3px; }
  .worker p { font-size: 11px; }
  .worker-dot { flex-shrink: 0; width: 8px; height: 8px; background: #718ba9; border-radius: 3px; box-shadow: 0 0 0 4px #e6edf5; }
  .worker-dot.secondary { background: #a08ab4; box-shadow: 0 0 0 4px #eee7f4; }
  .worker-note { display: block; text-align: center; font: 9px var(--mono); color: var(--flow-muted); margin-top: 13px; }
  .return-path { display: flex; gap: 11px; align-items: center; border-top: 1px dashed #d8cbb7; padding-top: 18px; color: var(--flow-muted); }
  .return-path > span { color: #a47931; font-size: 22px; }
  .return-path p { margin: 0; font-size: 12px; line-height: 1.65; }
  .return-path strong { font-weight: 600; color: var(--flow-ink); }
  .between-loops { display: flex; flex-direction: column; align-items: center; padding: 10px 0; gap: 5px; color: #9a8057; }
  .between-loops > span:first-child { height: 12px; width: 1px; background: #cdbc9e; }
  .between-loops p { margin: 0; font: 9px var(--mono); letter-spacing: .07em; text-transform: uppercase; }
  .between-loops > span:last-child { line-height: 1; }
  .quality .loop-number { background: #f1f4f8; border-color: #cfd8e5; color: var(--flow-ink); }
  .quality-layout { display: grid; grid-template-columns: minmax(0, 1fr) 55px 240px; align-items: center; margin: 32px 0 26px; }
  .iteration { border: 1px solid var(--flow-line); border-radius: 13px; padding: 23px; background: #fffdfa; }
  .lane-label { margin: 0 0 22px; color: var(--flow-muted); font: 9px var(--mono); letter-spacing: .055em; text-transform: uppercase; }
  .iteration-lane { display: grid; grid-template-columns: repeat(3, minmax(0, 1fr)); list-style: none; padding: 0; margin: 0; gap: 25px; }
  .iteration-lane li { position: relative; }
  .iteration-lane li:not(:last-child)::after { content: '→'; position: absolute; right: -17px; top: 37px; color: #baa17a; }
  .step-index { display: block; font: 10px var(--mono); color: #9e7636; margin-bottom: 14px; }
  .iteration-lane h4 { margin-bottom: 5px; }
  .iteration-lane p { margin: 0; font-size: 11px; color: var(--flow-muted); }
  .small-return { border-top: 1px dashed var(--flow-line); padding-top: 15px; margin-top: 23px; color: var(--flow-muted); font-size: 11px; }
  .small-return span { font-size: 16px; margin-right: 7px; color: #9e7636; }
  .review-connector { position: relative; height: 1px; background: #cdbc9e; }
  .qa-card { padding: 23px; border-radius: 13px; background: var(--flow-ink); color: #fffdfa; }
  .qa-seal { float: right; width: 25px; height: 25px; display: grid; place-items: center; border: 1px solid #c4a56e; border-radius: 50%; color: #f5c875; }
  .qa-card .node-meta { margin-top: 0; color: #f5c875; padding-right: 30px; line-height: 1.6; }
  .qa-card h4 { font: 600 25px/1.15 var(--serif); margin: 18px 0 12px; letter-spacing: -.03em; }
  .qa-card p { margin: 0; color: #c9d2df; font-size: 12px; line-height: 1.8; }
  .decision-paths { display: grid; grid-template-columns: 1fr 1fr; gap: 16px; }
  .decision { display: flex; align-items: flex-start; gap: 13px; padding: 20px; border-radius: 12px; border: 1px solid; }
  .repair { background: #fcf5f1; border-color: #ead9cd; }
  .deliver { background: #f2f7f1; border-color: #d8e3d3; }
  .decision-symbol { font-size: 24px; line-height: 1; color: #a56842; }
  .deliver .decision-symbol { color: #527549; }
  .decision-label { display: block; margin: 0 0 8px; font: 9px/1.5 var(--mono); letter-spacing: .035em; color: #8e5737; }
  .deliver .decision-label { color: #466740; }
  .decision h4 { margin-bottom: 5px; font-size: 14px; }
  .decision p { margin: 0; font-size: 12px; color: var(--flow-muted); line-height: 1.6; }
  .memory-note { display: flex; gap: 18px; align-items: center; max-width: 650px; margin: 27px auto 0; }
  .memory-note p { margin: 0; font-size: 12px; line-height: 1.7; color: var(--flow-muted); }
  .memory-note strong { color: var(--flow-ink); font-weight: 600; }
  .memory-icon { display: grid; gap: 4px; flex: 0 0 27px; }
  .memory-icon i { height: 5px; border: 1px solid #b59d77; border-radius: 2px; }
  @keyframes handoff { 0%, 15% { left: 0; opacity: 0; } 25%, 75% { opacity: 1; } 90%, 100% { left: calc(100% - 5px); opacity: 0; } }
  .motion-check:checked ~ .flow-panels *, .motion-check:checked ~ .flow-panels *::before, .motion-check:checked ~ .flow-panels *::after { animation-play-state: paused; }
  @media (max-width: 1000px) and (min-width: 701px) {
    .coordination-lane { gap: 20px; }
    .connector { width: 20px; }
    .node-card { padding: 18px 10px; min-height: 229px; }
    .worker { padding: 15px 9px; gap: 7px; }
    .worker-dot { display: none; }
    .quality-layout { grid-template-columns: minmax(0, 1fr) 30px 200px; }
    .panel-heading > p { display: none; }
  }
  @media (max-width: 700px) {
    .flow-panel { padding: 24px 20px; border-radius: 17px; }
    .panel-heading { display: block; }
    .panel-heading > p { margin-top: 16px; }
    .desktop-break { display: none; }
    .heading-title { gap: 12px; align-items: flex-start; }
    .loop-number { width: 34px; height: 34px; }
    .overline { font-size: 9px; }
    .coordination-lane { grid-template-columns: 1fr; gap: 29px; margin-top: 26px; }
    .node-card { min-height: 0; padding: 18px; }
    .node-mark { float: right; }
    .node-meta { margin-top: 0; padding-right: 40px; }
    .connector { left: 50%; top: 100%; height: 29px; width: 1px; }
    .connector::after { right: -3px; top: auto; bottom: 2px; transform: rotate(135deg); }
    .connector i { left: -2px; top: 0; animation-name: handoff-down; }
    .worker-pair { grid-template-columns: 1fr 1fr; gap: 10px; }
    .worker-pair::before, .worker-pair::after { display: none; }
    .worker { display: block; padding: 15px 10px; }
    .worker-dot { display: block; margin: 2px 0 13px 3px; }
    .worker h4 { font-size: 13px; }
    .quality-layout { grid-template-columns: 1fr; margin-top: 26px; }
    .iteration { padding: 18px 14px; }
    .iteration-lane { gap: 15px; }
    .iteration-lane h4 { font-size: 12px; }
    .iteration-lane p { font-size: 11px; }
    .iteration-lane li:not(:last-child)::after { right: -12px; font-size: 11px; }
    .review-connector { height: 29px; width: 1px; justify-self: center; }
    .review-connector::after { right: -3px; top: auto; bottom: 2px; transform: rotate(135deg); }
    .review-connector i { left: -2px; top: 0; animation-name: handoff-down; }
    .decision-paths { grid-template-columns: 1fr; gap: 12px; }
    .decision { padding: 18px; }
    .memory-note { align-items: flex-start; padding: 0 5px; }
    .memory-icon { margin-top: 6px; }
  }
  @keyframes handoff-down { 0%, 15% { top: 0; opacity: 0; } 25%, 75% { opacity: 1; } 90%, 100% { top: calc(100% - 5px); opacity: 0; } }
  @media (prefers-reduced-motion: reduce) {
    .flow-panels *, .flow-panels *::before, .flow-panels *::after { animation: none !important; }
    .motion-control { display: none; }
    .motion-check { display: none; }
  }
</style>
